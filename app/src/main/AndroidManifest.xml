<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.bluejack20_2.chantuy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chantuy">
        <activity android:name=".submitData.SubmitDataActivity"></activity>
        <activity android:name=".views.update_curhat.UpdateCurhatActivity" />
        <activity
            android:name=".views.login.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.insert_curhat.InsertCurhatActivity" />
        <activity android:name=".views.curhat_detail.CurhatDetailActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>